<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <Version>3.0.0</Version>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <PropertyGroup>
    <PackageId>JustSFTP.Client</PackageId>
    <Authors>RobIII, Bip901</Authors>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Title>JustSFTP.Client</Title>
    <Description>SFTP V3 Client Implementation</Description>
    <Copyright>(c) 2022 KeenSystems, 2026 Bip901</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <RepositoryUrl>https://github.com/Bip901/JustSFTP</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageProjectUrl>https://github.com/Bip901/JustSFTP</PackageProjectUrl>
    <PackageTags>sftp;sftp-client;sftpclient;ssh</PackageTags>
    <DocumentationFile>
			$(MSBuildProjectDirectory)/bin/$(Configuration)/$(TargetFramework)/JustSFTP.Client.xml</DocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SFTPProtocol\JustSFTP.Protocol.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FileAbstractions" Version="0.6.0" />
  </ItemGroup>
</Project>
